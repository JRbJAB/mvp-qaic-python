# 🧱 MVP QAIC — WebApp Architecture CDC

Statut: `CURRENT_FOLDER_ACTIVE`

Source canonique documentaire: fichiers `.md` avec emoji quand utile.

Structure:
- `CURRENT/` : CDC actif, schéma PNG stable, pack ZIP.
- `ARCHIVE/` : versions superseded et éléments cassés/obsolètes.

La WebApp Reflex doit utiliser le PNG stable comme asset visuel d’architecture.
Le SVG est utilisable seulement après validation XML.
